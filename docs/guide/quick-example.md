# 完整示例

以一段英文访谈为例：

1. 在设置中选择 WhisperX、MLX Whisper 模型和 English forced alignment 模型。
2. 导入视频，在转录页开启多人模式；单人内容则保持关闭。
3. 转录完成后检查实时预览中的重叠、过长、紧贴和空译文指标。
4. 进入字幕页，启用智能断句和中文翻译，选择已经测试通过的 LLM 配置。
5. 完成后检查原文、译文和时间轴，保存或导出双语 SRT。

处理流程不会创建新视频，也不会把字幕烧录到画面中。FFmpeg 仅负责音轨提取、采样率转换和媒体探测。
