# 工作流程

```text
媒体导入
  → FFprobe 读取媒体与音轨
  → FFmpeg 提取单声道 16 kHz 音频
  → 可选音频增强
  → ASR 转录
  → forced alignment 校准词级时间轴
  → 可选多人说话人聚类与交叉校验
  → 时间轴质量校验
  → 智能断句与文本优化
  → 可选翻译与错位审计
  → SRT / ASS / TXT / JSON 导出
```

各页面不会在任务中自动跳转。源媒体始终保持不变，输出以字幕文件和任务恢复文件为主。
