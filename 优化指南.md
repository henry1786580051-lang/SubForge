# SubForge 断句与翻译质量优化指南

版本：2026-08-30  
适用对象：任何能够读取和修改本地仓库、运行测试、调用已配置 LLM 的执行 harness  
当前仓库根目录：`/Users/guwenhan/Desktop/Project/Subtitle`  
默认本地样本根目录：`/Users/guwenhan/Desktop/YouTube`

## 1. 最终目标

本指南用于持续改进 SubForge 的断句与翻译质量，同时避免重新陷入“发现一个案例、增加一条规则、再被另一条案例推翻”的维护方式。

执行者必须同时保证：

1. 译文准确、自然、连贯，并具有与原视频语气相符的适度文采；
2. 字幕结构完整，不出现空译文、占位文字、思考内容、重复、遗漏或批量错位；
3. 单人、多人对话和混合语言模式互不破坏；
4. 新策略具有可重复的普适证据，不依赖某个历史句子、车型、地名或专名；
5. API 请求、Token、思考调用和耗时受到明确约束；
6. 新行为可独立关闭，失败后能完整退回当前稳定流程；
7. 更换执行 harness 后，能够仅依赖仓库、样本清单和本指南继续工作。

## 2. 当前架构状态

开始工作前必须理解以下事实：

- Phase 0-7 的结构重构已经进入代码；
- 不可变任务上下文、预计算特征、类型化诊断、规则注册表、模式策略、完整性保护、评估器、遥测和候选隔离均已采用；
- Phase 8 的行为候选和 Phase 9 的首次盲测已经完成；
- 所有行为候选均因收益不足、激活不稳定或效率超限而被拒绝；
- 被拒候选的运行代码、开关和候选测试已经移除；
- 当前产品继续使用稳定的 legacy 行为；
- Phase 10 产品发布阶段尚未进入。

因此，不能把“框架已重构”误解为“新翻译算法已经替换生产算法”。未来工作应利用新框架产生可靠证据，再决定是否引入一个新的行为候选。

## 3. 强制阅读文件

任何新 harness 开始工作时，必须按顺序读取：

1. 本指南：`优化指南.md`
2. 架构计划：`docs/dev/translation-quality-refactor-plan.md`
3. 执行手册：`docs/dev/translation-quality-refactor-execution-guide.md`
4. 翻译模块说明：`docs/dev/translate-module.md`
5. 项目配置：`pyproject.toml`
6. 当前工作区状态：运行 `git status --short`

不得只读取聊天摘要后直接修改算法。仓库文件是唯一持续有效的实现依据。

## 4. 文件与模块地图

下列路径均相对于仓库根目录。当前绝对路径可由
`/Users/guwenhan/Desktop/Project/Subtitle/<相对路径>` 得到。

### 4.1 生产入口与编排

| 职责 | 文件 |
| --- | --- |
| 字幕处理 API、任务启动和最终保存 | `backend/app/api/subtitle.py` |
| 翻译器创建与配置注入 | `subforge/core/translate/factory.py` |
| LLM 翻译、验证、重试和修复编排 | `subforge/core/translate/llm_translator.py` |
| 翻译任务上下文 | `subforge/core/translate/context.py` |
| 翻译基础接口 | `subforge/core/translate/base.py` |
| LLM 拆句入口 | `subforge/core/split/split_by_llm.py` |
| 拆句主流程 | `subforge/core/split/split.py` |
| 英文边界评分兼容层 | `subforge/core/split/boundary.py` |
| 字幕软限制与硬限制 | `subforge/core/split/length_policy.py` |

### 4.2 新质量架构

| 职责 | 文件或目录 |
| --- | --- |
| 不可变字幕任务快照 | `subforge/core/translate/quality/session.py` |
| 每条源字幕的确定性特征 | `subforge/core/translate/quality/features.py` |
| 单人、多人、单语言、混合语言策略 | `subforge/core/translate/quality/policies.py` |
| 稳定规则 ID 和类型化诊断 | `subforge/core/translate/quality/diagnostics.py` |
| 响应结构、完整性和译文硬约束 | `subforge/core/translate/quality/invariants.py` |
| 源文、词时间轴和说话人字段保护 | `subforge/core/translate/quality/integrity.py` |
| 数字与实体保留校验 | `subforge/core/translate/quality/preservation.py` |
| 修复计划与思考预算，目前主要用于观察 | `subforge/core/translate/quality/repair_planner.py` |
| 中文边界规则注册表 | `subforge/core/translate/quality/boundary_registry.py` |
| 中文边界纯检测器 | `subforge/core/translate/quality/chinese_boundary_detectors/` |
| 英文边界特征和注册表 | `subforge/core/split/boundary_features.py`、`subforge/core/split/boundary_registry.py` |
| 英文边界纯检测器 | `subforge/core/split/boundary_detectors/` |
| 候选版本、缓存和输出隔离 | `subforge/core/translate/quality/pipeline_identity.py` |
| 目标语言、占位符、照抄和思考泄漏检测 | `subforge/core/translate/quality/text.py` |
| 不保留字幕文本的专名证据计数 | `subforge/core/translate/quality/canonical_evidence.py` |

### 4.3 LLM 与 Prompt

| 职责 | 文件或目录 |
| --- | --- |
| 通用客户端、模型能力和思考参数 | `subforge/core/llm/client.py` |
| 结构化响应解析 | `subforge/core/llm/response.py` |
| 请求级效率遥测 | `subforge/core/llm/telemetry.py` |
| 安全日志 | `subforge/core/llm/request_logger.py` |
| 单人/多人翻译 Prompt | `subforge/core/prompts/translate/` |
| 拆句 Prompt | `subforge/core/prompts/split/` |
| LLM 供应商与本地配置 | `subforge/settings/llm.py`、`backend/app/api/config.py` |

Prompt 是最后修改项。能够通过确定性解析、特征、诊断或局部策略解决的问题，不应先扩大 Prompt。

### 4.4 评估 Harness

| 职责 | 文件或目录 |
| --- | --- |
| 样本发现、验证、静态基线和比较入口 | `scripts/evaluate_translation_quality.py` |
| 隔离运行真实翻译任务 | `scripts/run_translation_quality_shadow.py` |
| SRT 结构化解析 | `scripts/translation_quality/srt.py` |
| 样本清单、哈希与分组 | `scripts/translation_quality/manifest.py` |
| 静态质量指标 | `scripts/translation_quality/metrics.py` |
| 质量报告 | `scripts/translation_quality/report.py` |
| 候选与基线比较 | `scripts/translation_quality/comparison.py` |
| 请求、Token、缓存、思考和耗时聚合 | `scripts/translation_quality/efficiency.py` |
| 英文边界快照与审计 | `scripts/translation_quality/boundary_snapshot.py`、`boundary_audit.py` |
| 中文边界快照与审计 | `scripts/translation_quality/chinese_boundary_snapshot.py`、`chinese_boundary_audit.py` |

### 4.5 主要测试

- 翻译测试：`tests/test_translate/`
- 拆句测试：`tests/test_split/`
- 评估脚本测试：`tests/test_scripts/test_translation_quality_*.py`
- 候选运行器测试：`tests/test_scripts/test_run_translation_quality_shadow.py`
- LLM 客户端和思考策略测试：`tests/test_llm/`
- 打包清理保护测试：`tests/test_scripts/test_build_desktop.py`

## 5. 样本要求

### 5.1 每组样本必须具备的文件

优先使用同一视频的“三件套”：

1. **源文件**：词级或尽可能接近词级时间轴的转录 SRT；
2. **机器成品**：当前应用生成的 `_processed.srt` 或失败时的 `_recovery.srt`；
3. **人工参考**：ChatGPT Pro、其他高质量模型或人工完成的精修 SRT。

常见位置：

```text
/Users/guwenhan/Desktop/YouTube/<视频名称>/三件套/
```

样本文件不得复制进 Git 仓库。清单只记录路径、元数据和哈希。

### 5.2 人工参考并非绝对真值

人工参考可能存在：

- 广告段落被删除；
- 时间轴被人工调整；
- 多条字幕被合并或拆开；
- 风格偏好而非准确性修复；
- 人工漏改或误改。

因此比较必须基于连续语义窗口，不能要求机器字幕与人工字幕逐条一一对应。涉及原意和时间顺序时，以源 SRT 为第一依据。

### 5.3 模式覆盖

开发集应至少覆盖：

- 单人汽车评测；
- 双人或多人访谈；
- 英语为主的科普或工程视频；
- 混合语言视频；
- 长视频和长静音区；
- 有广告删除的人工参考；
- 有时间轴人工调整的参考文件。

发现覆盖缺口时先记录，不得用一个不匹配的样本代替整个模式。

## 6. 当前本地评估状态

约定的本地清单位置为：

```text
/Users/guwenhan/Desktop/Project/Subtitle/artifacts/translation-quality/corpus.local.json
```

截至本指南写入时，该清单和此前的评估报告已被旧版桌面构建清理逻辑删除，原始 YouTube 字幕样本未受影响。构建脚本现已修复，今后的 `--clean` 不再删除 `artifacts/`。

重要限制：

1. 不得假装旧清单仍然存在；
2. 不得凭记忆猜测旧 development、validation、holdout 分组；
3. 可以重新建立 development 清单并继续离线开发；
4. 下一次正式盲测必须重新选择未参与规则设计的新 holdout；
5. 已经查看或用于历史优化的样本不能重新宣称为盲测样本。

## 7. Harness 无关执行契约

任何执行者都必须遵守：

1. 工作目录固定为仓库根目录；
2. 先读取 `git status --short`，不得重置、覆盖或删除用户已有改动；
3. 不直接修改源 SRT、机器 SRT和人工参考 SRT；
4. 所有评估产物写入被 Git 忽略的 `artifacts/translation-quality/`；
5. API Key 只能从应用已保存的配置或系统安全存储读取，禁止打印、写入报告或提交；
6. 未经用户授权不得把字幕发送给付费 API；
7. 用户指定 GLM-5.3-Flash 时，只能使用 `zhipu/glm-5.3-flash`，不得静默切换 DeepSeek、MiMo、MiniMax、NVIDIA 或其他模型；
8. GLM-5.3-Flash 完整测试默认并发 `20`、批量 `20`；
9. 基线和候选必须使用相同模型、并发、批量、思考配置和缓存状态；
10. 每个行为候选只有一个明确 revision，不得在同一次候选运行中继续修改代码；
11. 长任务必须持续到完成或明确失败，不能遗留后台进程；
12. 更换 harness 时先读取状态文件，不要从聊天记录猜测进度。

## 8. 持久化状态文件

每轮工作创建：

```text
artifacts/translation-quality/runs/<run-id>/optimization-state.json
```

最低字段：

```json
{
  "schema_version": 1,
  "run_id": "YYYYMMDD-short-name",
  "status": "preflight|auditing|implemented|development-tested|rejected|holdout-ready|accepted",
  "git_revision": "commit hash or unknown",
  "working_tree_dirty": true,
  "manifest": "artifacts/translation-quality/corpus.local.json",
  "manifest_sha256": "sha256",
  "candidate_revision": "candidate-name-v1",
  "provider": "zhipu",
  "model": "glm-5.3-flash",
  "concurrency": 20,
  "batch_size": 20,
  "reflect": true,
  "completed_steps": [],
  "test_commands": [],
  "baseline_artifacts": {},
  "candidate_artifacts": {},
  "decision": "pending",
  "stop_reason": ""
}
```

状态文件不得包含字幕文本、Prompt、响应正文、API Key、Base URL 凭据或人物姓名列表。

## 9. 完整执行流程

### Step 0：环境预检

1. 确认仓库根目录；
2. 读取强制文档；
3. 记录 Git revision 和工作区改动；
4. 确认样本根目录存在；
5. 确认应用当前 LLM 配置为智谱 GLM，模型为 `glm-5.3-flash`；
6. 只确认 Key 是否可用，不得显示 Key；
7. 运行当前回归基线：

```bash
PYTHONPYCACHEPREFIX=/private/tmp/subforge-pycache \
UV_CACHE_DIR=/private/tmp/uv-cache \
uv run pytest -q -m "not integration" --tb=short

UV_CACHE_DIR=/private/tmp/uv-cache uv run ruff check .
UV_CACHE_DIR=/private/tmp/uv-cache uv run pyright
git diff --check
```

任何命令失败时先定位已有故障，不能在未知红色基线上开始算法实验。

### Step 1：建立或验证样本清单

设置样本根目录：

```bash
export SUBFORGE_TRANSLATION_CORPUS_ROOT=/Users/guwenhan/Desktop/YouTube
```

如果清单不存在，先发现所有三件套并生成临时 development 清单：

```bash
uv run python scripts/evaluate_translation_quality.py discover \
  --root "$SUBFORGE_TRANSLATION_CORPUS_ROOT" \
  --output artifacts/translation-quality/corpus.local.json
```

随后人工或通过可信元数据确认每组文件角色、说话人模式、语言、广告删除和时间轴编辑状态。正式冻结清单时：

- 使用 `--validation-id <sample-id>` 指定验证集；
- 仅对从未用于规则设计的新样本使用 `--holdout-id <sample-id>`；
- 在确定 holdout 前不得阅读其机器与人工差异；
- 分组确定后立即记录清单 SHA-256。

验证清单：

```bash
uv run python scripts/evaluate_translation_quality.py validate \
  --root "$SUBFORGE_TRANSLATION_CORPUS_ROOT" \
  --manifest artifacts/translation-quality/corpus.local.json
```

哈希不一致时停止。不要用 `--skip-hashes` 掩盖被修改的样本。

### Step 2：生成静态基线

只对 development 和 validation 生成可用于规则设计的报告：

```bash
uv run python scripts/evaluate_translation_quality.py baseline \
  --root "$SUBFORGE_TRANSLATION_CORPUS_ROOT" \
  --manifest artifacts/translation-quality/corpus.local.json \
  --split development \
  --split validation \
  --output-dir artifacts/translation-quality/baselines/<run-id>
```

报告中的人工修改条数只能说明差异规模，不能直接代表错误数。新模型运行可能改变分段，跨运行时不得把 cue 数量差异直接当作质量提升。

### Step 3：进行语义窗口审计

使用结构化 SRT 解析器，不得用正则或字符串切割临时解析 SRT。

对机器结果与人工参考的差异按连续语义窗口检查，分类为：

1. **结构完整性**：漏键、多键、空译文、占位符、格式损坏；
2. **源事实保留**：数字、单位、否定、比较关系、专名、实体；
3. **语义归属**：译文提前、延后、跨条错配、主语错配；
4. **边界质量**：主谓拆分、修饰语与中心词拆分、状语悬空、从句挂接错误；
5. **重复与遗漏**：相邻或跨窗口重复、源事实未覆盖；
6. **中文质量**：生硬直译、语法不通、衔接差、口吻不符、文采不足；
7. **多人模式**：说话人轮次、代词、省略、跨说话人误合并；
8. **混合语言**：语言片段被吞掉、误译、专名脚本错误；
9. **非算法差异**：广告删除、人工时间调整、纯风格偏好、人工误改；
10. **模型随机性**：无法在离线重放或多个独立样本中稳定复现。

不得只列举最显眼的几条。需要对整份 development 结果做同类扫描，并建立按规则 ID、样本和模式统计的证据矩阵。

### Step 4：判断是否具备普适性

行为候选必须同时满足：

- 至少在多个独立位置或多个 development 样本中出现；
- 能由确定性特征或稳定类型化诊断识别；
- 有明确反例和排除条件；
- 不依赖完整历史译文或领域专用句子；
- 不与现有规则重复；
- 可限定影响范围；
- 有明确 legacy fallback；
- 预期不会触发全批次重译或全篇思考。

单个句子只能建立观察记录，不能直接成为生产规则。

以下历史方向已经被拒绝，除非出现新的确定性证据，否则不得重试：

- 宽泛的专名 ownership 放行；
- 仅靠拼写相似度的 ASR canonical mapping；
- 通过放宽数字等价校验挽救局部译文；
- 稀疏 missing-key 的实时模型修复；
- 对 untranslated 诊断直接局部重译；
- 纯型号字幕的宽泛目标脚本豁免；
- 已完成盲测的 local-preservation repair。

### Step 5：先建立确定性重放

当前仓库已经具备评估和隔离框架，并已实现主翻译 agent loop 的响应重放。新的 harness 在提出行为候选前，应先使用并验证：

```text
scripts/translation_quality/replay.py                 # 捕获、响应与配置重放核心
scripts/replay_translation_quality.py                 # 命令行入口
tests/test_scripts/test_translation_quality_replay.py # 确定性、并发隔离、无网络测试
```

本地重放数据放在：

```text
artifacts/translation-quality/replay/<fixture-id>/
```

每个 fixture 至少保存：

- provider 和 model；
- pipeline revision；
- 阶段名称；
- 输入键集合和哈希；
- 原始响应的本地私密副本；
- 解析结果；
- 类型化诊断；
- legacy 后续动作；
- 请求配置中的思考模式；
- 不含密钥的 Token 与耗时元数据。

包含字幕或响应正文的 fixture 必须保持在 Git 忽略目录，报告只能输出计数和哈希。重放必须做到相同 fixture 多次运行产生字节一致的解析、诊断和决策结果。

没有可重复重放证据时，不得因为一次 API 运行偶然成功就采用新规则。

在隔离 API 运行器中添加 `--capture-replay`，会把真实主翻译循环的私密 fixture 放在该次运行目录的 `replay/` 内。随后执行：

```bash
uv run python scripts/replay_translation_quality.py \
  --fixtures artifacts/translation-quality/api-runs/<run-id>/replay \
  --output artifacts/translation-quality/runs/<run-id>/replay-report.json
```

该命令不调用 API，对每个 fixture 连续执行两次，比较请求顺序、解析与规范化结果、类型化诊断、返回或异常结果。输出路径必须不存在，防止覆盖冻结证据。

范围限制：目前只覆盖 `_agent_loop`，不覆盖 ASR、源断句、优化、文档级复核或供应商的网络重试。解析前原文与解析后对象只保存在私密 fixture；公共报告仅含哈希、计数、规则 ID。没有类型化诊断的 legacy 拒绝仍按反馈哈希保留，不能冒充已有完整类型覆盖。

#### 隔离候选与拒绝记录

`scripts/translation_quality/experiments.py` 只由评估脚本导入，不由应用导入。运行器的
`--experiment` 可显式启用候选；不指定时保持 legacy。候选名称会记录在 `run.json`，
恢复运行时必须完全一致。不得把候选隔离的 `pipeline_variant=candidate` 标签本身当作
生产行为已经升级的证明。

当前可复现的实验为：

- `exact-name-spacing`：只认可当前 cue 内完整拉丁词组的精确空格差异；不是拼写相似度豁免。
- `scoped-terminology`：限制术语生成时夹带局部动作、方向或数字的 Prompt 候选；尚未通过采用门槛。
- `unowned-fact-feedback`：只改进数字/型号归属错误的反馈，不改变接受、拒绝条件。

例如，在原有完整运行命令末尾追加 `--experiment exact-name-spacing`。重放脚本也支持
相同选项；此时退出码为零只表示确定性重放完成，不表示候选质量通过。必须逐项审查
`matches_capture=false` 的变化，确认只修复预期错误。

`scripts/probe_translation_repair_acceptance.py` 是独立的小窗口验收研究工具，不是应用
修复器。它调用保存的智谱 GLM 配置、关闭思考，并比较旧验收与前后版本比较验收；
人工预期只留在本地，不发给模型。当前实验表明，单纯增加前后比较及只读上下文，仍会
偏爱生硬直译，且增加 Token。不得未经新证据直接接入生产或宣称解决了修复退化。

2026-08-31 的逐项实验、失败证据与未解决事项位于：

```text
artifacts/translation-quality/runs/20260831-accord-candidates/
```

注意：无空译文、无占位符和结构校验通过，只是必要条件。已有实验仍存在自然度、语义
归属和数字单位问题；不得以更快的一次运行取代语义审阅，也不得把未触发候选的运行
收益归因于该候选。单样本开发实验和合成反例不能代替独立验证集与未见盲测。

从 SRT 重建的响应必须标记为 `srt_reconstruction`，测试合成响应标记为 `synthetic`。它们不能冒充真实 API 响应，也不能用来估算历史 Token、耗时或重试次数。

### Step 6：选择正确的代码落点

按问题类型修改，不得继续向上帝函数追加条件：

- 英文源断句特征：`subforge/core/split/boundary_detectors/`；
- 中文译文边界特征：`subforge/core/translate/quality/chinese_boundary_detectors/`；
- 新规则元数据：对应的 `boundary_registry.py`；
- 响应结构和完整性：`quality/invariants.py`；
- 源字段和时间轴保护：`quality/integrity.py`；
- 单人/多人/混合语言差异：`quality/policies.py`；
- 数字与实体校验：`quality/preservation.py`，修改前必须证明不是模型随机性；
- 修复策略选择：`quality/repair_planner.py`，先 shadow 观察，不直接执行；
- provider 特性：`core/llm/client.py`，不得影响其他供应商；
- Prompt：仅在前述确定性层无法表达时修改，并分别维护单人和多人版本。

纯检测器应无网络、无文件写入、无全局可变状态，并返回稳定规则 ID。

### Step 7：候选设计记录

实现前在状态文件或候选说明中写明：

```text
Candidate revision:
Problem category:
Stable rule ID:
Development evidence count:
Affected modes:
Required features:
Exclusions and negative cases:
Repair strategy:
Reasoning allowed: yes/no
Maximum affected cues per task:
Maximum additional attempts:
Legacy fallback:
Expected quality gain:
Expected cost change:
```

一次 revision 只能改变一种决策。结构整理与行为改变必须拆开。

### Step 8：单元测试

每个新规则至少包括：

- 正常正例；
- 相似但不应触发的反例；
- 空值、缺键和畸形输入；
- 单条与多条窗口；
- 单人和多人模式；
- 单语言和混合语言模式；
- 数字、专名、否定和标点边界；
- 幂等性；
- 修复失败后保持旧结果；
- legacy 未启用候选时行为不变；
- 候选缓存和文件名与 legacy 隔离。

建议先运行：

```bash
uv run pytest -q tests/test_translate tests/test_split -m "not integration" --tb=short
uv run pytest -q tests/test_scripts/test_translation_quality_*.py --tb=short
uv run pytest -q tests/test_scripts/test_run_translation_quality_shadow.py --tb=short
```

### Step 9：冻结 GLM-5.3-Flash 开发基线

应用当前配置必须满足：

```text
provider = zhipu
model = glm-5.3-flash
concurrency = 20
batch_size = 20
```

不得读取或打印 Key。运行器会验证当前激活配置与命令参数是否一致。

如果源码环境与已安装应用使用不同的配置文件，应显式添加 `--settings-file <已保存的应用配置路径>`。运行器只在进程内读取该配置快照，不修改持久设置，不跨供应商寻找可用 Key。macOS 钥匙串需要用户授权时应等待授权，不把引用字符串当密钥发送。本机已安装应用的配置可能在 `/Users/guwenhan/SubForge/settings.json`；先确认文件存在和供应商、模型，不输出凭据。

先运行没有行为变化的隔离 baseline revision：

```bash
uv run python scripts/run_translation_quality_shadow.py \
  --root "$SUBFORGE_TRANSLATION_CORPUS_ROOT" \
  --manifest artifacts/translation-quality/corpus.local.json \
  --output-dir artifacts/translation-quality/api-runs/<run-id>-legacy \
  --revision <run-id>-legacy-shadow \
  --provider zhipu \
  --model glm-5.3-flash \
  --concurrency 20 \
  --batch-size 20 \
  --capture-replay \
  --split development
```

该 revision 使用候选隔离命名，但在未启用新行为时必须保持 legacy 决策。完成后冻结代码、清单、配置和报告，不得在同一 baseline 目录覆盖运行。

### Step 10：运行单一候选

```bash
uv run python scripts/run_translation_quality_shadow.py \
  --root "$SUBFORGE_TRANSLATION_CORPUS_ROOT" \
  --manifest artifacts/translation-quality/corpus.local.json \
  --output-dir artifacts/translation-quality/api-runs/<run-id>-candidate \
  --revision <candidate-revision> \
  --provider zhipu \
  --model glm-5.3-flash \
  --concurrency 20 \
  --batch-size 20 \
  --split development
```

运行期间：

- 不得修改候选代码；
- 不得降低并发、批量或更换模型后继续宣称是同一比较；
- 发生限流应按供应商既有重试策略等待；
- 任务中断时只使用 `--resume` 恢复同一目录和完全相同配置；
- 不得把失败任务从统计中删除。

### Step 11：生成候选静态报告

对 baseline 和 candidate 各自生成报告。候选运行器会在输出目录生成 `candidate-manifest.json`，其数据根目录是该运行的 `workloads/`：

```bash
uv run python scripts/evaluate_translation_quality.py baseline \
  --root artifacts/translation-quality/api-runs/<run-id>-candidate/workloads \
  --manifest artifacts/translation-quality/api-runs/<run-id>-candidate/candidate-manifest.json \
  --split development \
  --output-dir artifacts/translation-quality/api-runs/<run-id>-candidate/static
```

baseline 目录使用相同方式生成。报告文件包括：

- `report.json`
- `aggregate.json`
- `sample-metrics.jsonl`
- `report.md`
- `environment.json`
- `run.json`

### Step 12：聚合效率并比较

收集每个运行目录下所有 `.telemetry.json`。必须逐个枚举，避免漏掉失败样本。分别运行：

```bash
uv run python scripts/evaluate_translation_quality.py aggregate-efficiency \
  --snapshot <sample-1.telemetry.json> \
  --snapshot <sample-2.telemetry.json> \
  --output artifacts/translation-quality/api-runs/<run-id>/efficiency.json
```

然后比较：

```bash
uv run python scripts/evaluate_translation_quality.py compare \
  --legacy-report <legacy-static>/report.json \
  --candidate-report <candidate-static>/report.json \
  --legacy-efficiency <legacy>/efficiency.json \
  --candidate-efficiency <candidate>/efficiency.json \
  --output-dir <candidate>/legacy-comparison \
  --fail-on-regression
```

不能只比较完成文件数量。必须同时检查工作负载身份、缓存状态、请求、尝试、Token、思考请求、耗时和修复 shadow 覆盖率。

目前部分请求阶段名称由 Prompt 关键词推断，包含 `global context` 的翻译请求可能被计入 `context`。不要直接将该标签下的全部 Token 当作“生成全局上下文”的成本；总量和显式模型身份可核验，阶段因果归因还需结合调用证据。没有旧任务遥测时，不得从旧 SRT 推断旧成本或编造提升百分比。

### Step 13：人工质量审计

静态门槛通过后，才能进行语义与文采审计。

人工或高质量审阅模型应在不知道哪个是候选的情况下比较：

- 源语义是否完整；
- 主谓、宾语、修饰语、时间状语是否归属正确；
- 指代、省略和上下文是否自然；
- 多人轮次是否串台；
- 中文是否像自然口语或旁白，而不是机翻；
- 文采是否提高但没有擅自添加信息；
- 字幕长度和阅读节奏是否合理；
- 广告删除等非算法差异是否被排除。

“更有文采”不能覆盖事实错误、主语错误、信息遗漏或时间轴破坏。

### Step 14：思考模式使用规则

普通翻译、格式校验、占位符处理、漏键处理和确定性规则不得开启原生思考。

思考只允许用于已经确认的高风险窗口，例如：

- 复杂从句重组；
- 中文语序导致的语义归属问题；
- 多人对话中的指代和省略；
- 有可靠上下文证据的专名或 ASR 修复；
- 普通非思考修复已经失败且预算仍允许。

高风险重写只能读取有限前后文，不得对全篇逐条开启思考。`quality/repair_planner.py` 当前仍是 shadow 规划与预算观察，并非生产调用的全局预算执行器；不能宣称 legacy 全部请求已经受其硬性限制。候选必须明确接入自己的执行预算，不能绕过预算直接增加调用。

## 10. 接受门槛

候选只有同时满足以下条件才可进入下一阶段：

### 10.1 硬质量门槛

- 字幕键和源字段完整性：100%；
- 时间轴和词级信息不被翻译阶段破坏；
- 空译文：0；
- 占位符：0；
- 思考内容泄漏：0；
- 无法解释的源文照抄：0；
- 新增相邻重复：0；
- 新增严重语义错误：0；
- 失败任务必须留下可恢复文件。

### 10.2 模式门槛

- 单人模式不回归；
- 多人模式的说话人归属和指代不回归；
- 混合语言片段不丢失；
- 不允许用单人样本的提升证明多人模式同样提升。

### 10.3 效率门槛

相对冻结 baseline：

- successful requests 不超过 105%；
- request attempts 不超过 105%；
- total tokens 不超过 105%；
- wall duration 不超过 105%；
- reasoning-enabled requests 原则上不得增加；
- 若思考增加，必须有单独批准且有显著人工偏好收益，不能在普通候选中默认放宽。

### 10.4 普适性门槛

- 规则在多个独立 development 证据上成立；
- 有可靠反例；
- 不含完整历史句子的固定译文；
- 不含只服务某个车型、项目或视频的条件；
- 不依赖已经完成的 holdout 内容调参。

## 11. 盲测流程

只有 development 和 validation 全部门槛通过后，才能冻结候选并进入盲测。

当前旧 holdout 已用于历史 Phase 9，不能再次用于规则设计。下一轮需要在规则设计前选择新的未见样本并冻结哈希。

盲测命令必须：

```bash
uv run python scripts/run_translation_quality_shadow.py \
  --root "$SUBFORGE_TRANSLATION_CORPUS_ROOT" \
  --manifest artifacts/translation-quality/corpus.local.json \
  --output-dir artifacts/translation-quality/api-runs/<run-id>-holdout \
  --revision <frozen-candidate-revision> \
  --provider zhipu \
  --model glm-5.3-flash \
  --concurrency 20 \
  --batch-size 20 \
  --split holdout \
  --blind-holdout
```

盲测保护要求：

- 必须运行完整 holdout，禁止 `--sample-id`；
- 控制台样本名称显示为 `[blind]`；
- 任务内部 stdout/stderr 被屏蔽；
- warnings 和错误详情脱敏；
- 日常分析只读取聚合报告；
- 不查看逐条字幕差异；
- 不根据盲测结果修改阈值、Prompt 或规则；
- 盲测只能产生“接受”或“拒绝”结论。

如果必须查看某个 holdout 的详细内容，应将其永久移入 development，并补充新的未见 holdout 后重新冻结。

## 12. 接受、拒绝与回退

### 12.1 接受

候选只有在开发集、验证集、盲测、人工偏好和效率门槛全部通过后，才可以标记为 Phase 10-ready。此时仍不能直接替换产品默认值。

### 12.2 拒绝

任何门槛失败时：

1. 记录拒绝原因和聚合指标；
2. 删除候选行为、revision 特判、工厂参数和候选专用测试；
3. 保留通用且行为中立的评估框架；
4. 保留被 Git 忽略的运行证据；
5. 重新运行完整回归；
6. 不降低门槛挽救候选；
7. 不在失败候选上继续叠加第二种修复。

### 12.3 Phase 10

只有候选通过盲测后才进入：

1. 开发环境手动启用；
2. 小范围用户可选启用；
3. 监控失败率、恢复率、空译文、重复、Token 和耗时；
4. 逐步设为默认，同时保留 legacy fallback；
5. 连续多个版本稳定后再考虑删除旧行为。

## 13. 强制停止条件

遇到下列任一情况立即停止候选推进：

- 不确定三件套文件角色；
- 样本哈希变化；
- 误把广告删除当成翻译遗漏；
- 规则只由一个历史句子支持；
- 需要硬编码完整译文；
- 类型化诊断与 legacy 判断无解释地冲突；
- 候选改变源文字段、词时间轴或说话人字段；
- 请求、Token 或耗时超过门槛；
- 思考模式扩散到普通翻译或全篇；
- 候选不能立即回退；
- holdout 内容已经影响规则设计；
- API 配置不是用户指定模型；
- 工作区存在无法安全兼容的未知改动；
- 测试、Ruff、Pyright 或差异检查失败。

停止不等于任务失败。正确动作是保留证据、回退候选并报告限制。

## 14. 最终验证命令

候选保留或回退后均需运行：

```bash
PYTHONPYCACHEPREFIX=/private/tmp/subforge-pycache \
UV_CACHE_DIR=/private/tmp/uv-cache \
uv run pytest -q -m "not integration" --tb=short

UV_CACHE_DIR=/private/tmp/uv-cache uv run ruff check .
UV_CACHE_DIR=/private/tmp/uv-cache uv run pyright
git diff --check
```

如果修改了前端或配置 wiring，再运行：

```bash
cd frontend
npm run test
npm run lint
npm run build
```

只有需要改变产品默认值时才要求打包验证。纯评估框架或被拒候选不需要构建安装包。

## 15. 最终汇报模板

```text
目标：
执行日期：
执行 harness：
Git revision：
工作区是否有既有改动：
样本清单路径与 SHA-256：
Development / validation / holdout 数量：
模式覆盖：

发现的问题类别：
被确认的普适性证据：
被排除的误判和原因：

候选 revision：
修改模块：
稳定规则 ID：
正例与反例测试：
Legacy fallback：

模型：zhipu/glm-5.3-flash
并发 / 批量：20 / 20
Baseline 请求 / Token / 思考请求 / 耗时：
Candidate 请求 / Token / 思考请求 / 耗时：
缓存状态与命中率：

硬质量门槛：
单人模式：
多人模式：
混合语言模式：
人工盲审偏好：

结论：保留 / 拒绝 / 仅观察
回退是否完成：
完整测试结果：
尚存风险：
下一步需要的新样本：
```

## 16. 给新 Harness 的启动指令

新执行者接管任务时，应使用以下顺序：

1. 读取 `优化指南.md`；
2. 读取两份 `translation-quality-refactor-*` 文档；
3. 读取最新 `optimization-state.json`；
4. 检查 Git 和后台进程；
5. 验证样本清单和哈希；
6. 确认当前阶段是结构工作、证据审计、候选实现、开发测试还是盲测；
7. 从最后一个已验证检查点继续；
8. 不重复已完成 API 测试；
9. 不恢复已拒绝候选；
10. 不把聊天中的暂时判断覆盖到仓库事实之上。

当状态文件缺失或与仓库不一致时，回到 Step 0 重新预检；不要猜测。

## 17. 核心判断原则

一次高质量优化的标准不是“某几条字幕看起来更好”，而是：

- 问题能够被稳定描述；
- 原因能够被确定性复现；
- 规则能够被准确触发并有明确反例；
- 单人、多人和混合语言都没有隐藏回归；
- 质量提升能够通过人工偏好和硬指标共同证明；
- 成本没有失控；
- 失败时可以完整删除候选而不破坏架构。

无法同时满足这些条件时，宁可保留当前稳定流程，也不要把不确定性带入产品。
