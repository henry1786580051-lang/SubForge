# SubForge 断句与翻译质量优化指南

版本：2026-09-05 / 分级验收 2.2

本版修订验收方法，不更改应用默认翻译流程，不自动恢复或批准历史候选。
对今后实验，本版第 10-12 节优先于两份架构文档中历史阶段的统一 5% 门槛。
旧实验的冻结协议和结论保留；新规则只能用于事先登记的新一轮实验。

2.2 补充版本与证据绑定、单项与组合验证、校验放宽的双向证明、分阶段验证和本机交付记录。
本次仅修订文档，不重新判定历史实验通过，也不触发新的模型调用或应用构建。

适用对象：任何能够读取和修改本地仓库、运行测试、调用已配置 LLM 的执行 harness

当前仓库根目录：`/Users/guwenhan/Desktop/Project/Subtitle`

默认本地样本根目录：`/Users/guwenhan/Desktop/YouTube`

## 1. 最终目标

本指南用于持续改进 SubForge 的断句与翻译质量，同时避免重新陷入“发现一个案例、增加一条规则、再被另一条案例推翻”的维护方式。

执行者必须同时保证：

1. 译文准确、自然、连贯，并具有与原视频语气相符的适度文采；
2. 字幕结构完整，不出现空译文、占位文字、思考内容、重复、遗漏或批量错位；
3. 单人、多人对话和混合语言模式互不破坏；
4. 新策略具有可重复的普适证据，不依赖某个历史句子、车型、地名或专名；
5. API 请求、Token、思考调用和耗时受到明确约束；
6. 新行为可独立关闭，失败后能完整退回当前稳定流程；
7. 更换执行 harness 后，能够仅依赖仓库、样本清单和本指南继续工作。

## 2. 当前架构状态

开始工作前必须理解以下事实：

- Phase 0-7 的结构重构已经进入代码；
- 不可变任务上下文、预计算特征、类型化诊断、规则注册表、模式策略、完整性保护、评估器、遥测和候选隔离均已采用；
- Phase 8 的行为候选和 Phase 9 的首次盲测已经完成；
- 历史 Phase 8/9 候选曾因收益不足、激活不稳定或效率超限而被拒绝；
- 历史候选的生产行为已回退；2026-08-31 的后续隔离研究见 Step 5，不能一概说所有实验代码均已删除；
- 当前仍使用 legacy 主流程，已叠加后续局部修复；不能理解为所有行为与历史基线完全相同；
- 历史完整流程候选的 Phase 10 尚未进入；本机局部更新与完整流程推广分别记录。

因此，不能把“框架已重构”误解为“新翻译算法已经替换生产算法”。未来工作应利用新框架产生可靠证据，再决定是否引入一个新的行为候选。

### 2.1 当前代码、实测与交付状态（2026-09-05）

| 层次 | 已确认事实 | 结论边界 |
| --- | --- | --- |
| 代码保留 | 本轮保留 RPM 等价校验、稳定诊断顺序、相同成功审查复用、保守弱边界排除；价格扩展已撤回 | 每项证据与待验证部分见 [局部优化记录](docs/dev/low-token-validation-fixes.md) |
| 完整 GLM 实测 | 两组正式配对的组合候选总 tokens 下降 2.39%，但语义审查未通过 | 实测候选含已撤回的价格扩展，不能代表最终保留版本 |
| 最终版本验证 | 2,690 项非集成测试通过，并完成离线回放 | 不代表完整翻译质量或总体成本通过 |
| 本机应用 | 用户授权后已更新并打开 `dist/SubForge.app` 1.2.1；签名、8 个变更模块核对和运行检查通过 | 本机交付不补足独立质量评审或效率证据 |
| 对外发布与总体验收 | 本轮未公开发布；未形成完整流程质量或最终版本效率验收通过结论 | 不以安装成功追认候选通过 |

本机交付证据为 `artifacts/desktop-update-20260905/result.json`；实验与冻结版本证据为
`artifacts/translation-quality/runs/20260905-low-token/`。日期和版本号不能代替实际文件、哈希与构建核对。

## 3. 强制阅读文件

任何新 harness 开始工作时，必须按顺序读取：

1. 本指南：`优化指南.md`
2. 架构计划：`docs/dev/translation-quality-refactor-plan.md`
3. 执行手册：`docs/dev/translation-quality-refactor-execution-guide.md`
4. 翻译模块说明：`docs/dev/translate-module.md`
5. 项目配置：`pyproject.toml`
6. 当前工作区状态：运行 `git status --short`

不得只读取聊天摘要后直接修改算法。仓库文件是唯一持续有效的实现依据。

## 4. 文件与模块地图

下列路径均相对于仓库根目录。当前绝对路径可由
`/Users/guwenhan/Desktop/Project/Subtitle/<相对路径>` 得到。

### 4.1 生产入口与编排

| 职责 | 文件 |
| --- | --- |
| 字幕处理 API、任务启动和最终保存 | `backend/app/api/subtitle.py` |
| 翻译器创建与配置注入 | `subforge/core/translate/factory.py` |
| LLM 翻译、验证、重试和修复编排 | `subforge/core/translate/llm_translator.py` |
| 翻译任务上下文 | `subforge/core/translate/context.py` |
| 翻译基础接口 | `subforge/core/translate/base.py` |
| LLM 拆句入口 | `subforge/core/split/split_by_llm.py` |
| 拆句主流程 | `subforge/core/split/split.py` |
| 英文边界评分兼容层 | `subforge/core/split/boundary.py` |
| 字幕软限制与硬限制 | `subforge/core/split/length_policy.py` |

### 4.2 新质量架构

| 职责 | 文件或目录 |
| --- | --- |
| 不可变字幕任务快照 | `subforge/core/translate/quality/session.py` |
| 每条源字幕的确定性特征 | `subforge/core/translate/quality/features.py` |
| 单人、多人、单语言、混合语言策略 | `subforge/core/translate/quality/policies.py` |
| 稳定规则 ID 和类型化诊断 | `subforge/core/translate/quality/diagnostics.py` |
| 响应结构、完整性和译文硬约束 | `subforge/core/translate/quality/invariants.py` |
| 源文、词时间轴和说话人字段保护 | `subforge/core/translate/quality/integrity.py` |
| 数字与实体保留校验 | `subforge/core/translate/quality/preservation.py` |
| 源文支持的转速单位等价 | `subforge/core/translate/quality/localized_quantities.py` |
| 有限完整句式的弱边界排除 | `subforge/core/translate/quality/closed_boundary.py` |
| 任务内相同成功审查复用 | `subforge/core/translate/quality/verdict_cache.py` |
| 修复计划与思考预算，目前主要用于观察 | `subforge/core/translate/quality/repair_planner.py` |
| 中文边界规则注册表 | `subforge/core/translate/quality/boundary_registry.py` |
| 中文边界纯检测器 | `subforge/core/translate/quality/chinese_boundary_detectors/` |
| 英文边界特征和注册表 | `subforge/core/split/boundary_features.py`、`subforge/core/split/boundary_registry.py` |
| 英文边界纯检测器 | `subforge/core/split/boundary_detectors/` |
| 候选版本、缓存和输出隔离 | `subforge/core/translate/quality/pipeline_identity.py` |
| 目标语言、占位符、照抄和思考泄漏检测 | `subforge/core/translate/quality/text.py` |
| 不保留字幕文本的专名证据计数 | `subforge/core/translate/quality/canonical_evidence.py` |

### 4.3 LLM 与 Prompt

| 职责 | 文件或目录 |
| --- | --- |
| 通用客户端、模型能力和思考参数 | `subforge/core/llm/client.py` |
| 结构化响应解析 | `subforge/core/llm/response.py` |
| 请求级效率遥测 | `subforge/core/llm/telemetry.py` |
| 安全日志 | `subforge/core/llm/request_logger.py` |
| 单人/多人翻译 Prompt | `subforge/core/prompts/translate/` |
| 拆句 Prompt | `subforge/core/prompts/split/` |
| LLM 供应商与本地配置 | `subforge/settings/llm.py`、`backend/app/api/config.py` |

按问题性质选择改动层，不预设 Prompt 永远最后：解析、数字和字段错误优先修复确定性代码；
语气、语篇衔接和自然度可以优先做范围清晰的 Prompt 实验。不得为了满足“确定性”要求，
硬造关键词补丁；也不得把本可直接修复的程序错误交给 LLM 反复审查。

### 4.4 评估 Harness

| 职责 | 文件或目录 |
| --- | --- |
| 样本发现、验证、静态基线和比较入口 | `scripts/evaluate_translation_quality.py` |
| 隔离运行真实翻译任务 | `scripts/run_translation_quality_shadow.py` |
| SRT 结构化解析 | `scripts/translation_quality/srt.py` |
| 样本清单、哈希与分组 | `scripts/translation_quality/manifest.py` |
| 静态质量指标 | `scripts/translation_quality/metrics.py` |
| 质量报告 | `scripts/translation_quality/report.py` |
| 候选与基线比较 | `scripts/translation_quality/comparison.py` |
| 分级静态分流、冻结预算读取 | `scripts/translation_quality/admission.py` |
| 请求、Token、缓存、思考和耗时聚合 | `scripts/translation_quality/efficiency.py` |
| 英文边界快照与审计 | `scripts/translation_quality/boundary_snapshot.py`、`boundary_audit.py` |
| 中文边界快照与审计 | `scripts/translation_quality/chinese_boundary_snapshot.py`、`chinese_boundary_audit.py` |

### 4.5 主要测试

- 翻译测试：`tests/test_translate/`
- 拆句测试：`tests/test_split/`
- 评估脚本测试：`tests/test_scripts/test_translation_quality_*.py`
- 候选运行器测试：`tests/test_scripts/test_run_translation_quality_shadow.py`
- LLM 客户端和思考策略测试：`tests/test_llm/`
- 打包清理保护测试：`tests/test_scripts/test_build_desktop.py`

## 5. 样本要求

### 5.1 每组样本必须具备的文件

优先使用同一视频的“三件套”：

1. **源文件**：词级或尽可能接近词级时间轴的转录 SRT；
2. **机器成品**：当前应用生成的 `_processed.srt` 或失败时的 `_recovery.srt`；
3. **人工参考**：ChatGPT Pro、其他高质量模型或人工完成的精修 SRT。

常见位置：

```text
/Users/guwenhan/Desktop/YouTube/<视频名称>/三件套/
```

样本文件不得复制进 Git 仓库。清单只记录路径、元数据和哈希。

### 5.2 人工参考并非绝对真值

人工参考可能存在：

- 广告段落被删除；
- 时间轴被人工调整；
- 多条字幕被合并或拆开；
- 风格偏好而非准确性修复；
- 人工漏改或误改。

因此比较必须基于连续语义窗口，不能要求机器字幕与人工字幕逐条一一对应。涉及原意和时间顺序时，以源 SRT 为第一依据。

### 5.3 模式覆盖

开发集应至少覆盖：

- 单人汽车评测；
- 双人或多人访谈；
- 英语为主的科普或工程视频；
- 混合语言视频；
- 长视频和长静音区；
- 有广告删除的人工参考；
- 有时间轴人工调整的参考文件。

发现覆盖缺口时先记录，不得用一个不匹配的样本代替整个模式。

## 6. 当前本地评估状态

约定的本地清单位置为：

```text
/Users/guwenhan/Desktop/Project/Subtitle/artifacts/translation-quality/corpus.local.json
```

历史清单曾被旧版桌面构建清理逻辑删除；原始 YouTube 字幕未受影响。
截至 2026-09-01，本地清单、20260831-accord 基线及候选报告已重新存在。
构建脚本的 `--clean` 已不再删除 `artifacts/`。每次仍需验证实际文件和哈希，不能把文档日期当作存在性证明。

重要限制：

1. 检查当前清单的实际内容，不得把重建清单当作原冻结清单；
2. 不得凭记忆猜测旧 development、validation、holdout 分组；
3. 可以重新建立 development 清单并继续离线开发；
4. 下一次正式盲测必须重新选择未参与规则设计的新 holdout；
5. 已经查看或用于历史优化的样本不能重新宣称为盲测样本。

## 7. Harness 无关执行契约

任何执行者都必须遵守：

1. 工作目录固定为仓库根目录；
2. 先读取 `git status --short`，不得重置、覆盖或删除用户已有改动；
3. 不直接修改源 SRT、机器 SRT和人工参考 SRT；
4. 所有评估产物写入被 Git 忽略的 `artifacts/translation-quality/`；
5. API Key 只能从应用已保存的配置或系统安全存储读取，禁止打印、写入报告或提交；
6. 未经用户授权不得把字幕发送给付费 API；
7. 用户指定 GLM-5.3-Flash 时，只能使用 `zhipu/glm-5.3-flash`，不得静默切换 DeepSeek、MiMo、MiniMax、NVIDIA 或其他模型；
8. GLM-5.3-Flash 完整测试默认并发 `20`、批量 `20`；
9. 基线和候选的模型、并发、批量、缓存策略等控制变量一致；若研究局部思考路由，其差异必须是唯一登记变量，不要求候选实际思考次数完全相同；
10. 每个行为候选只有一个明确 revision，不得在同一次候选运行中继续修改代码；
11. 长任务必须持续到完成或明确失败，不能遗留后台进程；
12. 更换 harness 时先读取状态文件，不要从聊天记录猜测进度。

## 8. 持久化状态文件

每轮工作创建：

```text
artifacts/translation-quality/runs/<run-id>/optimization-state.json
```

最低字段：

```json
{
  "schema_version": 1,
  "run_id": "YYYYMMDD-short-name",
  "status": "preflight|auditing|implemented|development-tested|observing|rejected|holdout-ready|accepted",
  "git_revision": "commit hash or unknown",
  "working_tree_dirty": true,
  "manifest": "artifacts/translation-quality/corpus.local.json",
  "manifest_sha256": "sha256",
  "candidate_revision": "candidate-name-v1",
  "admission_track": "bugfix|quality|efficiency",
  "admission_policy_path": "runs/<run-id>/admission-policy.json",
  "admission_policy_sha256": "hash recorded before candidate run",
  "affected_modes": ["single"],
  "adoption_scope": "local_bugfix|mode_specific|full_pipeline",
  "paired_repeat_plan": "baseline variability and repeat schedule",
  "provider": "zhipu",
  "model": "glm-5.3-flash",
  "concurrency": 20,
  "batch_size": 20,
  "reflect": true,
  "completed_steps": [],
  "test_commands": [],
  "baseline_artifacts": {},
  "candidate_artifacts": {},
  "decision": "pending",
  "stop_reason": ""
}
```

状态文件不得包含字幕文本、Prompt、响应正文、API Key、Base URL 凭据或人物姓名列表。

### 8.1 版本、证据与交付记录

新实验还须在状态文件或其引用的附录中登记以下信息。它们是执行记录约定，当前工具不自动强制验证；
不要把示例字段误认为已实现的新 schema，也不要为补字段改写旧冻结文件。

- `revision_lineage`：工作基线、各单项候选、组合候选、最终保留版本的身份、父版本与组成项。
- `code_manifest`：每个版本的冻结代码文件清单、逐文件哈希及清单哈希；脏工作区不能只用 Git commit 标识。
- `evidence_bindings`：每份报告对应的代码、样本与配置身份、验证阶段、审阅方式和适用范围。
- `retention_decisions`：逐项记录保留／撤回／观察、理由、适用证据、未验证结论及回退方法。
- `delivery_record`：本机路径、构建时间、版本、代码核对、签名与运行检查、备份和公开发布状态。

删减或撤回组合中的一项后，最终子集必须获得新身份。逐项注明旧证据为何仍适用；受影响调用链
重新回归。旧完整运行的 token、耗时和质量结论只属于旧版本，不能直接迁移或按比例扣除。
尚未实测最终子集时写“未单独实测”，不强制为补一个百分比追加付费运行。

安装授权、代码保留、实验结束和质量采用结论分开记录。旧实验结论保留；后续安装记为有日期的
新事件，不把原先“截至当时未打包”的记录抹去。

## 9. 完整执行流程

### Step 0：环境预检

1. 确认仓库根目录；
2. 读取强制文档；
3. 记录 Git revision 和工作区改动；
4. 确认样本根目录存在；
5. 确认应用当前 LLM 配置为智谱 GLM，模型为 `glm-5.3-flash`；
6. 只确认 Key 是否可用，不得显示 Key；
7. 运行当前回归基线：

```bash
PYTHONPYCACHEPREFIX=/private/tmp/subforge-pycache \
UV_CACHE_DIR=/private/tmp/uv-cache \
uv run pytest -q -m "not integration" --tb=short

UV_CACHE_DIR=/private/tmp/uv-cache uv run ruff check .
UV_CACHE_DIR=/private/tmp/uv-cache uv run pyright
git diff --check
```

任何命令失败时先定位已有故障，不能在未知红色基线上开始算法实验。
若失败已确认与本轮无关，记录固定失败清单并验证修改前后没有新增失败；
不必为修一个独立 Bug 顺带重构全库。与本轮相关的回归或无法解释的失败必须先处理。

### Step 1：建立或验证样本清单

设置样本根目录：

```bash
export SUBFORGE_TRANSLATION_CORPUS_ROOT=/Users/guwenhan/Desktop/YouTube
```

如果清单不存在，先发现所有三件套并生成临时 development 清单：

```bash
uv run python scripts/evaluate_translation_quality.py discover \
  --root "$SUBFORGE_TRANSLATION_CORPUS_ROOT" \
  --output artifacts/translation-quality/corpus.local.json
```

随后人工或通过可信元数据确认每组文件角色、说话人模式、语言、广告删除和时间轴编辑状态。正式冻结清单时：

- 使用 `--validation-id <sample-id>` 指定验证集；
- 仅对从未用于规则设计的新样本使用 `--holdout-id <sample-id>`；
- 在确定 holdout 前不得阅读其机器与人工差异；
- 分组确定后立即记录清单 SHA-256。

验证清单：

```bash
uv run python scripts/evaluate_translation_quality.py validate \
  --root "$SUBFORGE_TRANSLATION_CORPUS_ROOT" \
  --manifest artifacts/translation-quality/corpus.local.json
```

哈希不一致时停止。不要用 `--skip-hashes` 掩盖被修改的样本。

### Step 2：生成静态基线

只对 development 和 validation 生成可用于规则设计的报告：

```bash
uv run python scripts/evaluate_translation_quality.py baseline \
  --root "$SUBFORGE_TRANSLATION_CORPUS_ROOT" \
  --manifest artifacts/translation-quality/corpus.local.json \
  --split development \
  --split validation \
  --output-dir artifacts/translation-quality/baselines/<run-id>
```

报告中的人工修改条数只能说明差异规模，不能直接代表错误数。新模型运行可能改变分段，跨运行时不得把 cue 数量差异直接当作质量提升。

### Step 3：进行语义窗口审计

使用结构化 SRT 解析器，不得用正则或字符串切割临时解析 SRT。

对机器结果与人工参考的差异按连续语义窗口检查，分类为：

1. **结构完整性**：漏键、多键、空译文、占位符、格式损坏；
2. **源事实保留**：数字、单位、否定、比较关系、专名、实体；
3. **语义归属**：译文提前、延后、跨条错配、主语错配；
4. **边界质量**：主谓拆分、修饰语与中心词拆分、状语悬空、从句挂接错误；
5. **重复与遗漏**：相邻或跨窗口重复、源事实未覆盖；
6. **中文质量**：生硬直译、语法不通、衔接差、口吻不符、文采不足；
7. **多人模式**：说话人轮次、代词、省略、跨说话人误合并；
8. **混合语言**：语言片段被吞掉、误译、专名脚本错误；
9. **非算法差异**：广告删除、人工时间调整、纯风格偏好、人工误改；
10. **模型随机性**：无法在离线重放或多个独立样本中稳定复现。

不得只列举最显眼的几条。需要对整份 development 结果做同类扫描，并建立按规则 ID、样本和模式统计的证据矩阵。

### Step 4：判断是否具备普适性

先登记路线：确定性 Bug 修复（bugfix）、质量提升（quality）、效率优化（efficiency）。
共通要求：

- 有明确反例和排除条件；
- 不依赖完整历史译文或领域专用句子；
- 不与现有规则重复；
- 可限定影响范围；
- 有明确 legacy fallback；
- 预期不会触发全批次重译或全篇思考。

Bug 路线：一个真实失败输入加可重复逻辑证明、边界和反例测试，可以证明程序缺陷。
例如整数格式化误删有效的 0，不需要等待多个视频再次碰巧触发。
质量路线：自然度、指代、语序与文采不要求存在正则判定器，应在独立语义窗口和样本上
进行盲审，证明收益不是单个句子的偏好。领域语义类别允许有专门策略，但不能硬编码某一期内容。
效率路线：先证明输出质量不劣，再证明资源或时间减少。

下列是历史失败方向，不是永久禁止研究的题目。重新研究前必须说明旧失败原因、
新的机制或证据、采用哪条路线，并生成新 revision；不能只放宽阈值或重复付费碰运气：

- 宽泛的专名 ownership 放行；
- 仅靠拼写相似度的 ASR canonical mapping；
- 通过放宽数字等价校验挽救局部译文；
- 稀疏 missing-key 的实时模型修复；
- 对 untranslated 诊断直接局部重译；
- 纯型号字幕的宽泛目标脚本豁免；
- 已完成盲测的 local-preservation repair。

### Step 5：先建立确定性重放

当前仓库已经具备评估和隔离框架，并已实现主翻译 agent loop 的响应重放。新的 harness 在提出行为候选前，应先使用并验证：

```text
scripts/translation_quality/replay.py                 # 捕获、响应与配置重放核心
scripts/replay_translation_quality.py                 # 命令行入口
tests/test_scripts/test_translation_quality_replay.py # 确定性、并发隔离、无网络测试
```

本地重放数据放在：

```text
artifacts/translation-quality/replay/<fixture-id>/
```

每个 fixture 至少保存：

- provider 和 model；
- pipeline revision；
- 阶段名称；
- 输入键集合和哈希；
- 原始响应的本地私密副本；
- 解析结果；
- 类型化诊断；
- legacy 后续动作；
- 请求配置中的思考模式；
- 不含密钥的 Token 与耗时元数据。

包含字幕或响应正文的 fixture 必须保持在 Git 忽略目录，报告只能输出计数和哈希。重放必须做到相同 fixture 多次运行产生字节一致的解析、诊断和决策结果。

确定性修复优先提供重放证据；重放证明的是程序决策，不是修改 Prompt 后模型质量会提高。
涉及新请求或 Prompt 的候选，需要真实配对运行和独立质量评审。
没有覆盖某个阶段的重放工具时，先建立该阶段最小固定输入测试，不必为局部修复重建整套平台。

在隔离 API 运行器中添加 `--capture-replay`，会把真实主翻译循环的私密 fixture 放在该次运行目录的 `replay/` 内。随后执行：

```bash
uv run python scripts/replay_translation_quality.py \
  --fixtures artifacts/translation-quality/api-runs/<run-id>/replay \
  --output artifacts/translation-quality/runs/<run-id>/replay-report.json
```

该命令不调用 API，对每个 fixture 连续执行两次，比较请求顺序、解析与规范化结果、类型化诊断、返回或异常结果。输出路径必须不存在，防止覆盖冻结证据。

范围限制：目前只覆盖 `_agent_loop`，不覆盖 ASR、源断句、优化、文档级复核或供应商的网络重试。解析前原文与解析后对象只保存在私密 fixture；公共报告仅含哈希、计数、规则 ID。没有类型化诊断的 legacy 拒绝仍按反馈哈希保留，不能冒充已有完整类型覆盖。

#### 隔离候选与拒绝记录

`scripts/translation_quality/experiments.py` 只由评估脚本导入，不由应用导入。运行器的
`--experiment` 可显式启用候选；不指定时保持 legacy。候选名称会记录在 `run.json`，
恢复运行时必须完全一致。不得把候选隔离的 `pipeline_variant=candidate` 标签本身当作
生产行为已经升级的证明。

当前可复现的实验为：

- `exact-name-spacing`：只认可当前 cue 内完整拉丁词组的精确空格差异；不是拼写相似度豁免。
- `scoped-terminology`：限制术语生成时夹带局部动作、方向或数字的 Prompt 候选；尚未通过采用门槛。
- `unowned-fact-feedback`：只改进数字/型号归属错误的反馈，不改变接受、拒绝条件。

例如，在原有完整运行命令末尾追加 `--experiment exact-name-spacing`。重放脚本也支持
相同选项；此时退出码为零只表示确定性重放完成，不表示候选质量通过。必须逐项审查
`matches_capture=false` 的变化，确认只修复预期错误。

`scripts/probe_translation_repair_acceptance.py` 是独立的小窗口验收研究工具，不是应用
修复器。它调用保存的智谱 GLM 配置、关闭思考，并比较旧验收与前后版本比较验收；
人工预期只留在本地，不发给模型。当前实验表明，单纯增加前后比较及只读上下文，仍会
偏爱生硬直译，且增加 Token。不得未经新证据直接接入生产或宣称解决了修复退化。

2026-08-31 的逐项实验、失败证据与未解决事项位于：

```text
artifacts/translation-quality/runs/20260831-accord-candidates/
```

注意：无空译文、无占位符和结构校验通过，只是必要条件。已有实验仍存在自然度、语义
归属和数字单位问题；不得以更快的一次运行取代语义审阅，也不得把未触发候选的运行
收益归因于该候选。单样本开发实验和合成反例不能代替独立验证集与未见盲测。

从 SRT 重建的响应必须标记为 `srt_reconstruction`，测试合成响应标记为 `synthetic`。它们不能冒充真实 API 响应，也不能用来估算历史 Token、耗时或重试次数。

### Step 6：选择正确的代码落点

按问题类型修改，不得继续向上帝函数追加条件：

- 英文源断句特征：`subforge/core/split/boundary_detectors/`；
- 中文译文边界特征：`subforge/core/translate/quality/chinese_boundary_detectors/`；
- 新规则元数据：对应的 `boundary_registry.py`；
- 响应结构和完整性：`quality/invariants.py`；
- 源字段和时间轴保护：`quality/integrity.py`；
- 单人/多人/混合语言差异：`quality/policies.py`；
- 数字与实体校验：`quality/preservation.py`，先用固定输入区分程序误判与模型输出变化；
- 修复策略选择：`quality/repair_planner.py`，先 shadow 观察，不直接执行；
- provider 特性：`core/llm/client.py`，不得影响其他供应商；
- Prompt：用于语篇、自然度或口吻等适合模型处理的任务，并明确单人和多人策略边界。

纯检测器应无网络、无文件写入、无全局可变状态，并返回稳定规则 ID。

### Step 7：候选设计记录

实现前在状态文件或候选说明中写明：

```text
Candidate revision:
Admission track / adoption scope:
Frozen policy path / SHA-256:
Problem category:
Stable rule ID:
Development evidence count:
Affected modes:
Required features:
Exclusions and negative cases:
Repair strategy:
Reasoning allowed: yes/no
Maximum affected cues per task:
Maximum additional attempts:
Legacy fallback:
Expected quality gain:
Expected cost change:
Primary quality outcome / minimum useful gain:
Baseline variability / paired repeat plan:
Affected and protected modes:
Final reviewer / evidence locations:
```

一次 revision 只能改变一种决策。结构整理与行为改变必须拆开。

组合验证使用单独的组合 revision，并列出已经逐项验证的组成 revision；不得把多个尚未验证的
决策藏在一个“效率优化”名称下。先给每项建立固定输入、反例和调用链证据，再检查组合交互。
不机械要求每项都完整翻译一遍视频；需要模型质量或节省幅度结论的项，再按对应路线追加实测。

记录实际触发、复用命中、避免的请求和保留的错误分别有多少。触发不等于正确修复，整体降本
不等于每项都有效；零命中只说明本轮未观察到收益。组合有收益也不能抵消某一项确认有害。

### Step 8：单元测试

根据影响范围选择测试，并注明不适用项，不能机械要求每个微小修改穷举所有模式。
风险检查清单包括：

- 正常正例；
- 相似但不应触发的反例；
- 空值、缺键和畸形输入；
- 单条与多条窗口；
- 单人和多人模式；
- 单语言和混合语言模式；
- 数字、专名、否定和标点边界；
- 幂等性；
- 修复失败后保持旧结果；
- legacy 未启用候选时行为不变；
- 候选缓存和文件名与 legacy 隔离。

建议先运行：

```bash
uv run pytest -q tests/test_translate tests/test_split -m "not integration" --tb=short
uv run pytest -q tests/test_scripts/test_translation_quality_*.py --tb=short
uv run pytest -q tests/test_scripts/test_run_translation_quality_shadow.py --tb=short
```

### Step 8.1：按证据决定是否升级验证

| 阶段 | 升级条件 | 停止或保留观察的条件 |
| --- | --- | --- |
| 固定输入与反例 | 错误机制明确，相关正反例、取消与回退检查通过 | 存在可复现误放行或未解决程序缺陷时先修复 |
| 离线回放与调用链 | 重放稳定、变化能解释，明确哪些真实请求阶段未覆盖 | 未触发或范围未覆盖时标未知，不把保存响应数换算为真实成本 |
| 必要的小范围真实调用 | 剩余问题确实涉及模型输出或在线路由，且发送授权和预算已明确 | 发现有害放行、无效路由或成本失控时不扩展到整片 |
| 预登记的完整配对 | 前面证据支持继续，质量目标、次数、顺序、预算和停止条件已冻结 | 按计划结束；临界结果按预案处理，不持续重跑直到满意 |

已有同版本、同范围的有效证据可复用，注明来源与适用性；不为遵守表格重复调用。
小范围实测不能代替需要完整语境的质量验证，完整配对也不能代替独立质量审阅。
发现预试缺陷时保留原始运行和成本；修订作为新版本记录，禁止看完成本后挑选性排除不利运行。

### Step 9：冻结 GLM-5.3-Flash 开发基线

先按第 10 节登记路线、预算、收益要求、复测计划和采用范围。GLM 是以下命令的示例模型；
用户明确指定其他模型时，统一替换基线和候选配置并取得对应发送授权，不得沿用旧示例误调模型。
预算必须在候选运行前冻结。基线本身的重复运行用于估计波动，不能拿一次最快的基线当稳定标准。

应用当前配置必须满足：

```text
provider = zhipu
model = glm-5.3-flash
concurrency = 20
batch_size = 20
```

不得读取或打印 Key。运行器会验证当前激活配置与命令参数是否一致。

如果源码环境与已安装应用使用不同的配置文件，应显式添加 `--settings-file <已保存的应用配置路径>`。运行器只在进程内读取该配置快照，不修改持久设置，不跨供应商寻找可用 Key。macOS 钥匙串需要用户授权时应等待授权，不把引用字符串当密钥发送。本机已安装应用的配置可能在 `/Users/guwenhan/SubForge/settings.json`；先确认文件存在和供应商、模型，不输出凭据。

先运行没有行为变化的隔离 baseline revision：

```bash
uv run python scripts/run_translation_quality_shadow.py \
  --root "$SUBFORGE_TRANSLATION_CORPUS_ROOT" \
  --manifest artifacts/translation-quality/corpus.local.json \
  --output-dir artifacts/translation-quality/api-runs/<run-id>-legacy \
  --revision <run-id>-legacy-shadow \
  --provider zhipu \
  --model glm-5.3-flash \
  --concurrency 20 \
  --batch-size 20 \
  --capture-replay \
  --split development
```

该 revision 使用候选隔离命名，但在未启用新行为时必须保持 legacy 决策。完成后冻结代码、清单、配置和报告，不得在同一 baseline 目录覆盖运行。

### Step 10：运行单一候选

```bash
uv run python scripts/run_translation_quality_shadow.py \
  --root "$SUBFORGE_TRANSLATION_CORPUS_ROOT" \
  --manifest artifacts/translation-quality/corpus.local.json \
  --output-dir artifacts/translation-quality/api-runs/<run-id>-candidate \
  --revision <candidate-revision> \
  --provider zhipu \
  --model glm-5.3-flash \
  --concurrency 20 \
  --batch-size 20 \
  --split development
```

运行期间：

- 不得修改候选代码；
- 不得降低并发、批量或更换模型后继续宣称是同一比较；
- 发生限流应按供应商既有重试策略等待；
- 任务中断时只使用 `--resume` 恢复同一目录和完全相同配置；
- 不得把失败任务从统计中删除。

### Step 11：生成候选静态报告

对 baseline 和 candidate 各自生成报告。候选运行器会在输出目录生成 `candidate-manifest.json`，其数据根目录是该运行的 `workloads/`：

```bash
uv run python scripts/evaluate_translation_quality.py baseline \
  --root artifacts/translation-quality/api-runs/<run-id>-candidate/workloads \
  --manifest artifacts/translation-quality/api-runs/<run-id>-candidate/candidate-manifest.json \
  --split development \
  --output-dir artifacts/translation-quality/api-runs/<run-id>-candidate/static
```

baseline 目录使用相同方式生成。报告文件包括：

- `report.json`
- `aggregate.json`
- `sample-metrics.jsonl`
- `report.md`
- `environment.json`
- `run.json`

### Step 12：聚合效率并比较

收集每个运行目录下所有 `.telemetry.json`。必须逐个枚举，避免漏掉失败样本。分别运行：

```bash
uv run python scripts/evaluate_translation_quality.py aggregate-efficiency \
  --snapshot <sample-1.telemetry.json> \
  --snapshot <sample-2.telemetry.json> \
  --output artifacts/translation-quality/api-runs/<run-id>/efficiency.json
```

然后比较：

```bash
uv run python scripts/evaluate_translation_quality.py compare \
  --legacy-report <legacy-static>/report.json \
  --candidate-report <candidate-static>/report.json \
  --legacy-efficiency <legacy>/efficiency.json \
  --candidate-efficiency <candidate>/efficiency.json \
  --policy artifacts/translation-quality/runs/<run-id>/admission-policy.json \
  --policy-sha256 <候选运行前记录的SHA256> \
  --output-dir <candidate>/legacy-comparison \
  --fail-on-regression
```

不能只比较完成文件数量。必须同时检查工作负载身份、缓存状态、请求、尝试、Token、思考请求、耗时和修复 shadow 覆盖率。

新版命令是静态分流，不是自动采用：`review` 表示进入质量审查，`observe` 表示预算、风险
或证据待核实，`blocked` 表示先解决新增硬信号或不可比条件。退出码见第 10.5 节。
省略 `--policy` 的旧命令保留 v1 行为，仅用于历史复现。不得让旧 5% 结果否决新版实验。

目前部分请求阶段名称由 Prompt 关键词推断，包含 `global context` 的翻译请求可能被计入 `context`。不要直接将该标签下的全部 Token 当作“生成全局上下文”的成本；总量和显式模型身份可核验，阶段因果归因还需结合调用证据。没有旧任务遥测时，不得从旧 SRT 推断旧成本或编造提升百分比。

### Step 13：人工质量审计

先排查文件损坏、源字段破坏等安全问题，随后并行审查质量和成本。
超过预算、未翻译风险或相似度风险，不能阻止语义与文采审查；审查可能证明风险是误报，
也可能证明候选虽有收益但需要进一步压缩开销。确认严重退化后停止推广，不靠文采抵消。

人工或高质量审阅模型应在不知道哪个是候选的情况下比较：

- 源语义是否完整；
- 主谓、宾语、修饰语、时间状语是否归属正确；
- 指代、省略和上下文是否自然；
- 多人轮次是否串台；
- 中文是否像自然口语或旁白，而不是机翻；
- 文采是否提高但没有擅自添加信息；
- 字幕长度和阅读节奏是否合理；
- 广告删除等非算法差异是否被排除。

“更有文采”不能覆盖事实错误、主语错误、信息遗漏或时间轴破坏。

先做准确性审阅，再做自然度、连贯性、口吻与文采偏好。使用同一连续语义窗口，随机交换 A/B，
允许平局；分别记录改进、退化、等价、无法判断。不让审阅者看到候选名、成本或预期结论。
高质量模型审阅不等于人类真值；严重分歧回看源文/音频。既抽查被修改窗口，也抽查未修改窗口。
按视频分别汇总，不能把同一视频里几十条相邻字幕当作几十个独立样本。

### Step 14：思考模式使用规则

普通翻译、格式校验、占位符处理、漏键处理和确定性规则不得开启原生思考。

思考只允许用于已经确认的高风险窗口，例如：

- 复杂从句重组；
- 中文语序导致的语义归属问题；
- 多人对话中的指代和省略；
- 有可靠上下文证据的专名或 ASR 修复；
- 普通非思考修复已经失败且预算仍允许。

高风险重写只能读取有限前后文，不得对全篇逐条开启思考。`quality/repair_planner.py` 当前仍是 shadow 规划与预算观察，并非生产调用的全局预算执行器；不能宣称 legacy 全部请求已经受其硬性限制。候选必须明确接入自己的执行预算，不能绕过预算直接增加调用。

记录新增思考的触发原因、Token、耗时及有效修复数。只增加一次思考不自动失败，
但没有因果收益也不允许扩大调用范围。候选执行上限、取消和失败回退必须可测试。

## 10. 接受门槛

这里区分研究推进、局部修复采用和完整流程替换。三者不能共用“必须解决一切且成本不增”的门槛。
静态比较器只提供证据分流，不测量信达雅，也不自动批准上线。

### 10.1 不可妥协的安全底线

- 候选不得破坏其输入的键、源文、词时间轴、说话人和语言字段；断句阶段允许合法重组，但须证明词覆盖、顺序和边界来源完整。
- 不得新增经复核确认的严重事实错误、主语错配、信息遗漏、重复、说话人串台或混合语言丢失。
- 空译文、占位符、思考泄漏不能作为完整成功成品交付；失败必须保留可恢复文件并明确状态。
- 不得泄露密钥、覆盖用户原始样本、调用错误模型或破坏回退。

应把缺陷区分为：候选新增、基线遗留、候选修复、检测误报、无法归因。
局部修复不必一次清除全产品的遗留问题，但遗留问题必须报告，不能伪装成完整成功。
新硬信号先阻止推广并核实；风险误报不得靠删除检测器或静默豁免来“通过”。
有限测试中未发现新增严重错误，只能按所测范围报告，不能宣称数学意义上的零风险。

### 10.2 三条验收路线

| 路线 | 必要证据 | 不应误设的门槛 |
| --- | --- | --- |
| bugfix：确定性 Bug | 固定真实输入、错误机制、修复前后重放、边界和反例、受影响调用链回归 | 不要求多个视频偶然触发同一错误；不因整次 API 的随机变化删除已证明的纯代码修复 |
| quality：质量提升 | 独立样本、盲审胜/平/负、事实与自然度分开、实质修改工作量减少、确认触发和负例 | 不要求每个资源指标都下降；不以“没有空白”代替质量提升 |
| efficiency：效率优化 | 在预先约定的质量非劣标准内，主要成本或耗时有可重复下降 | 不能以更快掩盖事实、完整性、边界或文采明显退化 |

Bug 修复若改变 Prompt、触发更多在线重写或放宽语义校验，应拆分改动；涉及概率质量的部分
仍按 quality 路线验证。不能给复杂翻译策略贴 bugfix 标签来绕过独立验证。
结构重构单独按行为等价路线处理，不宣称翻译质量提高。

### 10.2.1 校验放宽的双向证明

接受范围扩大会影响模型输出的去留，不能只凭“代码是确定性的”归为普通 bugfix。
先说明等价关系是否能由源文、数值、单位和语义归属限定；涉及近似价位、指代、语义容差等
概率判断的部分，按 quality 路线验证。

至少同时验证：

1. 正确表达不被误拒：有源文依据、数量与单位对应、归属明确，并覆盖适用的格式变体。
2. 错误表达不因局部匹配而放行：覆盖正确值旁夹带错误值、无源文依据的自我纠正、单位或数量级
   变化、否定／比较反转、跨条归属错误，以及与正常词语混淆的近似形式。

每项选择与规则相关的负例，并注明不适用范围；不要求单个校验器解决所有语义问题。
找到了一个正确片段，只能证明该片段的对应关系，不能证明整句无新增错误；原有其他检查仍须执行。
若反例证明新路径放过错误，撤回或重新设计该路径，保留复现证据，不追加只针对该句的黑名单。

### 10.3 影响范围与质量证据

登记影响模式：single（单人）、dialogue（多人）、mixed（混合语言），以及修改阶段。
单人专属策略可以只在单人启用，验证其他模式不会进入新路径；不能因此声称多人同样提升。
共享解析器、数字保护、客户端或通用 Prompt 修改需要更广的模式回归。
要求独立验证，不要求每个局部修复先做全语言、全模型、全领域大规模盲测。

quality 路线至少区分：事实正确性、语义归属、边界阅读感、自然口吻、文采、人工修改负担。
在实验前约定本轮主要改善项、最低实用收益和不可接受退化，不临时挑有利指标。
单个样本只能说明局部效果；跨样本结论应记录独立视频数量和不同模式表现。
保留未触发、退化和无法判断的样本；对稀有 Bug 可以补合成负例，但不得冒充独立真实视频。

### 10.4 成本、时间与思考预算

不再默认将调用数、尝试数、Token、耗时四项统一卡在 105%。在候选运行前登记：

1. 主要资源指标和上限：至少总 Token、完整任务耗时；必要时增加请求或思考 Token 上限。
2. 质量收益要求：为什么这项改善值得这些额外开销。
3. 基线波动和复测计划：固定样本/配置，交错执行 A/B 与 B/A，临界结论按预案复测。
4. 思考范围：仅高风险有限窗口、次数/Token 上限、超限回退，不扩散到全篇。
5. 预算基线为零或很小时的绝对增量；不得将 0 次到 1 次直接判作无限回归。

预算值由本轮目标和实测基线决定，不把 5% 简单替换成另一个万能百分比。
例如可以提议允许质量候选适度增本，但须在运行前写明并确认，不得先看结果后调预算。
成本以供应商真实计费字段和缓存价格为准；没有价格或缓存命中字段则标未知，不估造免费收益。
思考次数仅是诊断指标，不等同于思考 Token、成本或收益。

报告完整耗时、重试/限流等待、API 时间及处理阶段时间。并发请求的累计等待会重叠，
不能直接用总耗时减去 retry_wait_ms 当作“纯计算时间”；也不能把本策略增加的重试一律归咎网络。
记录中位数、范围和配对差值；样本足够才估计区间，不强行用小样本给统计显著性结论。
触发失败的测试也计入任务完成率和成本，不仅比较成功的较小工作负载。

超过预算先标 observing，完成质量审查，再选择降本、补证据或以新协议开启新实验。
安全漏洞和确认严重回归不能用成本或文采补偿；预算超限也不能未经批准直接上线。

### 10.5 可执行的新版静态分流

比较工具在显式传入 schema-2 policy 时启用新分流，不修改生产算法。
将以下结构保存到 `artifacts/translation-quality/runs/<run-id>/admission-policy.json`。
这是**格式示例，不是已经批准的通用预算**；运行前根据本轮目标确定具体值：

```json
{
  "schema_version": 2,
  "track": "quality",
  "candidate_revision": "candidate-name-v1",
  "affected_modes": ["single"],
  "budgets": {
    "tokens": {"max_ratio": 1.10, "absolute_allowance": 0},
    "wall_duration_ms": {"max_ratio": 1.15, "absolute_allowance": 5000},
    "reasoning_tokens": {"max_ratio": 1.10, "absolute_allowance": 2000}
  }
}
```

计算方式：候选上限 = 基线值 × max_ratio + absolute_allowance，单位与指标相同。
可选预算字段：successful_requests、request_attempts、reasoning_tokens、reasoning_enabled_requests。
供应商不提供某个预算字段时不能当作 0 通过，应记未知并选择可验证的预算。
当前历史遥测可能把供应商未上报的思考 Token 记为 0；静态工具只能识别报告中缺失的字段，
不能自动识别这种占位零。执行者必须核对原始 usage 的字段存在性，不把占位零作为“没有思考消耗”的证据。
数字必须有限且非负，未知字段或缺少 Token/耗时预算会报错。

用 `shasum -a 256 <policy文件>` 计算哈希，在候选运行前写入状态文件，再将该哈希传给 Step 12。
哈希只验证文件未改，不证明冻结时间；必须保留运行前的状态记录和 revision 对照。
工具不会自动核实用户审批、候选 revision 与实验记录的绑定，执行者必须核对。

带 `--fail-on-regression` 时：

- `review` / 退出 0：可进入正式人工/模型盲审，**不是质量通过或允许上线**。
- `observe` / 退出 2：预算超限、遗留缺陷、风险信号或证据不足；继续审查、归档或按预案补测，不自动删除。
- `blocked` / 退出 3：逐样本硬信号增加或输入/工作负载不一致；先排查，确认是否误报及因果关系。
- 无效配置、哈希或报告：工具报错，不产生有效采用判断。

JSON 中 `accepted` 和原始 `gates` 仍表示历史 v1 静态结论，便于复现旧报告；
新版看 `admission.decision`，且 `admission.production_adoption` 永远为 false。
不要把任何自动退出码当作信达雅证明。

工具仅筛查单组报告，尚不自动完成重复运行统计、严重语义裁决、逐词源字段证明、
真人偏好或真实思考范围核验。`required_review` 明确列出后续检查项，执行者不得跳过。
结构匹配指标比较的是机器与人工分段，不能独立证明翻译破坏了源时间轴。
未翻译/重复启发式只转入审查，不能直接等同于错误。空译文等按样本检查，防止总量改善掩盖局部失败。

## 11. 盲测流程

质量策略或完整流程替换，在 development 和独立 validation 上有可重复收益、未发现确认的
严重回归，并完成预算审阅后，再冻结候选进入盲测。局部确定性 Bug 不机械要求新整视频
holdout；固定输入证明、独立负例、相关模式回归和适当集成测试可形成独立的 bugfix 采用记录。

当前旧 holdout 已用于历史 Phase 9，不能再次用于规则设计。下一轮需要在规则设计前选择新的未见样本并冻结哈希。

盲测命令必须：

```bash
uv run python scripts/run_translation_quality_shadow.py \
  --root "$SUBFORGE_TRANSLATION_CORPUS_ROOT" \
  --manifest artifacts/translation-quality/corpus.local.json \
  --output-dir artifacts/translation-quality/api-runs/<run-id>-holdout \
  --revision <frozen-candidate-revision> \
  --provider zhipu \
  --model glm-5.3-flash \
  --concurrency 20 \
  --batch-size 20 \
  --split holdout \
  --blind-holdout
```

盲测保护要求：

- 必须运行完整 holdout，禁止 `--sample-id`；
- 控制台样本名称显示为 `[blind]`；
- 任务内部 stdout/stderr 被屏蔽；
- warnings 和错误详情脱敏；
- 日常分析只读取聚合报告；
- 开发调参者不查看逐条字幕差异；预先指定的独立审阅者可在匿名 A/B 下审阅，结果只用于冻结候选验收，不回流调参；
- 不根据盲测结果修改阈值、Prompt 或规则；
- 盲测可以产生“接受”“拒绝”“证据不足”；未触发、样本不足或服务故障不自动证明策略无效。

复测次数、样本追加条件在运行前约定，不能不停增加测试直到出现想要的结论。
静态指标全为零不说明没有自然度收益，也不说明有；盲审必须独立覆盖质量目标。

如果开发调参者必须查看某个 holdout 的详细内容并据此修改候选，应将其永久移入 development，
补充新的未见 holdout 后重新冻结；不得继续宣称旧集合盲测未污染。

## 12. 接受、拒绝与回退

### 12.1 接受

采用判断由证据审阅作出，工具不自动决定：

- bugfix：缺陷和修复有确定性证明，反例及相关链路回归通过，范围内未发现新增严重问题，可单独采用局部修复；不得顺带替换整套算法。
- quality：独立盲审确认准确性守住、主要质量目标有实用提升、保护模式不回归、开销符合事前协议。
- efficiency：独立质量评审不劣，主要效率目标的改善超过已观察的运行波动，其他资源符合预算。

完整流程替换还需要完整盲测和 Phase 10 渐进发布；模式专属改进只在验证范围内启用。
局部修复允许声明仍有遗留问题；最终交付的未完成字幕仍须标 recovery，不能虚报成功。

### 12.2 拒绝

先分类，不再把所有未通过都等同于拒绝：

1. 确认严重退化或无收益且明显增本：拒绝该 revision，关闭生产入口并保留原因。
2. 有收益但预算临界、样本不足、未触发或疑似误报：标 observing，不上线，不因未通过立刻删除。
3. 明确程序 Bug 与复杂策略混在一起：拆成独立 revision 和证据，不能把纯 Bug 修复与不可靠策略捆绑生死。
4. 保留有价值的复现用例、回归测试和精简隔离实现；生产入口不得导入未经采用的实验。
5. 临时日志、音频等按既有清理规则处理；证据和通用测试不是垃圾，敏感正文只留在忽略目录。
6. 保留旧协议和结论，新预算只能用于新实验，不事后把旧失败重写成成功。
7. 对候选做下一版修改时明确改动原因，不无边界叠加第二种修复；复跑受影响测试。

### 12.3 Phase 10

完整流程候选通过盲测，或局部修复通过其路线验收后，按对应范围进入：

1. 开发环境手动启用；
2. 小范围用户可选启用；
3. 监控失败率、恢复率、空译文、重复、Token 和耗时；
4. 逐步设为默认，同时保留 legacy fallback；
5. 连续多个版本稳定后再考虑删除旧行为。

用户明确要求更新本机应用时，按授权范围完成构建与运行核验，并登记真实安装状态。
若总体质量或效率仍未通过，交付记录必须同时保留这一结论；安装操作不自动改变实验采用状态，
也不授权对外发布。说明回退是可运行时关闭单项、代码回退，还是恢复旧应用，不能相互冒充。

## 13. 强制停止条件

遇到下列任一情况立即停止候选推进：

- 不确定三件套文件角色；
- 样本哈希变化；
- 误把广告删除当成翻译遗漏；
- 语义策略只由一个历史句子的固定译法支持，且没有独立证据；确定性 Bug 按 bugfix 路线处理；
- 需要硬编码完整译文；
- 类型化诊断与 legacy 判断无解释地冲突；
- 候选改变源文字段、词时间轴或说话人字段；
- 消耗达到事先批准的安全停止上限，或出现失控重试；单次比较略超效率目标则转观察，不删除证据；
- 思考模式扩散到普通翻译或全篇；
- 候选不能立即回退；
- holdout 内容已经影响规则设计；
- API 配置不是用户指定模型；
- 工作区存在无法安全兼容的未知改动；
- 本轮新增或相关测试、Ruff、Pyright、差异检查失败且无法解释。

停止推广不等于方案无效。正确动作是保留证据、确保生产仍可回退，并按 blocked / observing / rejected 区分原因。

## 14. 最终验证命令

候选代码保留或回退后，完成适用的最终检查：

```bash
PYTHONPYCACHEPREFIX=/private/tmp/subforge-pycache \
UV_CACHE_DIR=/private/tmp/uv-cache \
uv run pytest -q -m "not integration" --tb=short

UV_CACHE_DIR=/private/tmp/uv-cache uv run ruff check .
UV_CACHE_DIR=/private/tmp/uv-cache uv run pyright
git diff --check
```

如果修改了前端或配置 wiring，再运行：

```bash
cd frontend
npm run test
npm run lint
npm run build
```

改变产品默认值或用户明确要求更新应用时，执行打包验证。纯评估框架或被拒候选不需要构建安装包。
仅更新指南或报告时检查文档、引用与差异即可，
无需重跑已通过的算法测试、付费 API 或构建。已有检查只在代码身份与覆盖范围仍适用时复用。

## 15. 最终汇报模板

```text
目标：
执行日期：
执行 harness：
Git revision：
工作区是否有既有改动：
样本清单路径与 SHA-256：
Development / validation / holdout 数量：
模式覆盖：

发现的问题类别：
被确认的普适性证据：
被排除的误判和原因：

候选 revision：
单项 / 组合 / 最终保留版本与代码清单哈希：
实测报告绑定版本 / 撤回后可复用与失效的证据：
验收路线 / 采用范围：
预算文件 / 运行前冻结 SHA-256：
修改模块：
稳定规则 ID：
正例与反例测试：
Legacy fallback：

模型：zhipu/glm-5.3-flash
并发 / 批量：20 / 20
Baseline 请求 / Token / 思考请求 / 耗时：
Candidate 请求 / Token / 思考请求 / 耗时：
缓存状态与命中率：
基线波动 / 配对复测中位数与范围：
限流等待 / 无法分离的网络影响：
候选触发数 / 确认有效修复数：
复用命中 / 可证明避免的请求 / 无法归因的变化：

硬质量门槛：
单人模式：
多人模式：
混合语言模式：
人工盲审偏好：
事实、自然度、文采分别的胜/平/负：
新增缺陷 / 遗留缺陷 / 修复 / 误报 / 无法归因：

工具静态分流：review / observe / blocked（不是采用结论）
最终证据结论：局部采用 / 范围内试用 / 拒绝 / 继续观察
采用批准与证据位置：
回退是否完成：
完整测试结果：
逐项保留理由 / 已证明收益 / 待验证部分：
本机应用路径 / 构建身份 / 签名、代码核对与运行检查：
本机安装 / 对外发布 / 质量采用状态（分别记录）：
实际回退方式与备份：
尚存风险：
下一步需要的新样本：
```

## 16. 给新 Harness 的启动指令

新执行者接管任务时，应使用以下顺序：

1. 读取 `优化指南.md`；
2. 读取两份 `translation-quality-refactor-*` 文档；
3. 读取最新 `optimization-state.json`；
4. 检查 Git 和后台进程；
5. 验证样本清单和哈希；
6. 确认当前阶段是结构工作、证据审计、候选实现、开发测试还是盲测；
7. 从最后一个已验证检查点继续；
8. 不无目的重复已完成 API 测试；按事先登记的复测计划验证随机波动不是重复浪费；
9. 不自动恢复已拒绝候选；有新机制或证据时可登记新实验，不改写旧结论；
10. 不把聊天中的暂时判断覆盖到仓库事实之上。

当状态文件缺失或与仓库不一致时，回到 Step 0 重新预检；不要猜测。

## 17. 核心判断原则

一次高质量优化的标准不是“某几条字幕看起来更好”，而是：

- 问题能够被稳定描述；
- 程序缺陷能用固定输入证明；概率质量改进能用独立配对证据支持；
- 策略有明确适用范围和反例，不靠单个历史句子硬编码；
- 所影响模式经过验证，未影响模式有隔离和回归证据；
- 质量提升不依赖与精修版逐字相同，事实正确性、自然度、文采分别审阅；
- 额外成本与实际收益符合事先登记的协议，不失控；
- 未通过时可关闭或归档候选，保留有价值的证据和测试，不破坏生产。

不能确认收益时先保持生产稳定，但把“还不知道”与“已经证明有害”分开。
目标是有证据地改好产品，而不是让所有改动因为无法同时改善每一个指标而永久停留在旧流程。
